version: '3'
services:
  mysql:
    build:
      context: .
      dockerfile: Dockerfile.mysql
    container_name: mysql_container
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=Ldf44JRt9PE1eNR
      - MYSQL_DATABASE=app_db
      - MYSQL_USER=app_user
      - MYSQL_PASSWORD=Kws6yURw7UD1eVW
      - MYSQL_HOST=mysql
    networks:
      - db_network

  todoapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
      - MYSQL_DATABASE=app_db
      - MYSQL_USER=app_user
      - MYSQL_PASSWORD=Kws6yURw7UD1eVW
      - MYSQL_HOST=mysql
    networks:
      - db_network
    depends_on:
      - mysql
    restart: unless-stopped

networks:
  db_network:
    driver: bridge
volumes:
  db_data:
